import * as topicLineStyleUtils from "../../render/topiclinestyle/utils";
import * as brushes from "../../render/brushes";

/* harmony default export */
export const brace3 = Object(topicLineStyleUtils.braceLineStyleTemplate)({
  verticalBrush: brushes.brace3Vertical,
  taperedVerticalBrush: brushes.taperedBrace3Vertical,
});
