# I18N.md - 国际化设计

> 思维导图编辑器国际化支持设计

---

## 一、国际化架构

### 1.1 语言包结构

```typescript
// @y-mindmap/i18n/types.ts

interface LocaleMessages {
  // 通用
  common: {
    ok: string
    cancel: string
    confirm: string
    save: string
    delete: string
    edit: string
    close: string
    loading: string
    error: string
    success: string
  }
  
  // 菜单
  menu: {
    file: string
    edit: string
    view: string
    insert: string
    format: string
    help: string
  }
  
  // 工具栏
  toolbar: {
    new: string
    open: string
    save: string
    export: string
    undo: string
    redo: string
    cut: string
    copy: string
    paste: string
    selectAll: string
    addSubTopic: string
    addSiblingTopic: string
    deleteNode: string
    zoomIn: string
    zoomOut: string
    fitToContent: string
  }
  
  // 节点
  node: {
    centralTopic: string
    mainTopic: string
    subTopic: string
    floatingTopic: string
    callout: string
  }
  
  // 布局
  layout: {
    mindMap: string
    logicChart: string
    treeChart: string
    orgChart: string
    fishbone: string
    timeline: string
    spreadsheet: string
  }
  
  // 提示
  tooltip: {
    addSubTopic: string
    addSiblingTopic: string
    deleteNode: string
    foldNode: string
    unfoldNode: string
  }
  
  // 错误
  error: {
    invalidOperation: string
    nodeNotFound: string
    permissionDenied: string
    saveFailed: string
    loadFailed: string
  }
}
```

### 1.2 语言包实现

```typescript
// @y-mindmap/i18n/locales/zh-CN.ts

export const zhCN: LocaleMessages = {
  common: {
    ok: '确定',
    cancel: '取消',
    confirm: '确认',
    save: '保存',
    delete: '删除',
    edit: '编辑',
    close: '关闭',
    loading: '加载中...',
    error: '错误',
    success: '成功',
  },
  
  menu: {
    file: '文件',
    edit: '编辑',
    view: '视图',
    insert: '插入',
    format: '格式',
    help: '帮助',
  },
  
  toolbar: {
    new: '新建',
    open: '打开',
    save: '保存',
    export: '导出',
    undo: '撤销',
    redo: '重做',
    cut: '剪切',
    copy: '复制',
    paste: '粘贴',
    selectAll: '全选',
    addSubTopic: '添加子节点',
    addSiblingTopic: '添加兄弟节点',
    deleteNode: '删除节点',
    zoomIn: '放大',
    zoomOut: '缩小',
    fitToContent: '适应内容',
  },
  
  node: {
    centralTopic: '中心主题',
    mainTopic: '主要主题',
    subTopic: '子主题',
    floatingTopic: '浮动主题',
    callout: '标注',
  },
  
  layout: {
    mindMap: '思维导图',
    logicChart: '逻辑图',
    treeChart: '树形图',
    orgChart: '组织图',
    fishbone: '鱼骨图',
    timeline: '时间线',
    spreadsheet: '表格',
  },
  
  tooltip: {
    addSubTopic: '添加子节点 (Tab)',
    addSiblingTopic: '添加兄弟节点 (Enter)',
    deleteNode: '删除节点 (Delete)',
    foldNode: '折叠节点',
    unfoldNode: '展开节点',
  },
  
  error: {
    invalidOperation: '无效操作',
    nodeNotFound: '节点不存在',
    permissionDenied: '权限不足',
    saveFailed: '保存失败',
    loadFailed: '加载失败',
  },
}
```

---

## 二、国际化管理器

### 2.1 管理器实现

```typescript
// @y-mindmap/i18n/i18n-manager.ts

class I18nManager {
  private currentLocale: string = 'zh-CN'
  private messages: Map<string, LocaleMessages> = new Map()
  private listeners: Set<(locale: string) => void> = new Set()
  
  constructor() {
    // 注册默认语言
    this.registerLocale('zh-CN', zhCN)
    this.registerLocale('en-US', enUS)
  }
  
  /**
   * 注册语言包
   */
  registerLocale(locale: string, messages: LocaleMessages): void {
    this.messages.set(locale, messages)
  }
  
  /**
   * 设置当前语言
   */
  setLocale(locale: string): void {
    if (!this.messages.has(locale)) {
      throw new Error(`Locale not found: ${locale}`)
    }
    
    this.currentLocale = locale
    this.notifyListeners()
  }
  
  /**
   * 获取当前语言
   */
  getLocale(): string {
    return this.currentLocale
  }
  
  /**
   * 翻译
   */
  t(key: string, params?: Record<string, string>): string {
    const messages = this.messages.get(this.currentLocale)
    if (!messages) return key
    
    const keys = key.split('.')
    let value: any = messages
    
    for (const k of keys) {
      value = value?.[k]
      if (value === undefined) return key
    }
    
    if (typeof value !== 'string') return key
    
    // 替换参数
    if (params) {
      return Object.entries(params).reduce(
        (str, [key, val]) => str.replace(`{${key}}`, val),
        value
      )
    }
    
    return value
  }
  
  /**
   * 监听语言变更
   */
  onLocaleChange(listener: (locale: string) => void): () => void {
    this.listeners.add(listener)
    return () => this.listeners.delete(listener)
  }
  
  private notifyListeners(): void {
    this.listeners.forEach(listener => listener(this.currentLocale))
  }
}
```

---

## 三、日期时间

### 3.1 日期格式化

```typescript
// @y-mindmap/i18n/date.ts

class DateFormatter {
  private locale: string
  
  constructor(locale: string) {
    this.locale = locale
  }
  
  /**
   * 格式化日期
   */
  format(date: Date, format: 'short' | 'medium' | 'long' = 'medium'): string {
    const options: Intl.DateTimeFormatOptions = this.getOptions(format)
    return new Intl.DateTimeFormat(this.locale, options).format(date)
  }
  
  /**
   * 格式化相对时间
   */
  formatRelative(date: Date): string {
    const now = new Date()
    const diff = now.getTime() - date.getTime()
    
    const seconds = Math.floor(diff / 1000)
    const minutes = Math.floor(seconds / 60)
    const hours = Math.floor(minutes / 60)
    const days = Math.floor(hours / 24)
    
    if (seconds < 60) return '刚刚'
    if (minutes < 60) return `${minutes}分钟前`
    if (hours < 24) return `${hours}小时前`
    if (days < 7) return `${days}天前`
    
    return this.format(date, 'short')
  }
  
  private getOptions(format: string): Intl.DateTimeFormatOptions {
    switch (format) {
      case 'short':
        return { year: 'numeric', month: '2-digit', day: '2-digit' }
      case 'medium':
        return { year: 'numeric', month: 'short', day: 'numeric' }
      case 'long':
        return { year: 'numeric', month: 'long', day: 'numeric', weekday: 'long' }
      default:
        return {}
    }
  }
}
```

---

## 四、数字格式

### 4.1 数字格式化

```typescript
// @y-mindmap/i18n/number.ts

class NumberFormatter {
  private locale: string
  
  constructor(locale: string) {
    this.locale = locale
  }
  
  /**
   * 格式化数字
   */
  format(value: number, options?: Intl.NumberFormatOptions): string {
    return new Intl.NumberFormat(this.locale, options).format(value)
  }
  
  /**
   * 格式化百分比
   */
  formatPercent(value: number): string {
    return new Intl.NumberFormat(this.locale, {
      style: 'percent',
      minimumFractionDigits: 0,
      maximumFractionDigits: 2,
    }).format(value)
  }
  
  /**
   * 格式化货币
   */
  formatCurrency(value: number, currency: string = 'CNY'): string {
    return new Intl.NumberFormat(this.locale, {
      style: 'currency',
      currency,
    }).format(value)
  }
}
```

---

## 五、文本方向

### 5.1 RTL 支持

```typescript
// @y-mindmap/i18n/direction.ts

class TextDirection {
  /**
   * 获取文本方向
   */
  getDirection(locale: string): 'ltr' | 'rtl' {
    const rtlLocales = ['ar', 'he', 'fa', 'ur']
    const lang = locale.split('-')[0]
    return rtlLocales.includes(lang) ? 'rtl' : 'ltr'
  }
  
  /**
   * 检查是否为 RTL
   */
  isRTL(locale: string): boolean {
    return this.getDirection(locale) === 'rtl'
  }
  
  /**
   * 应用方向
   */
  applyDirection(element: HTMLElement, locale: string): void {
    element.dir = this.getDirection(locale)
  }
}
```
